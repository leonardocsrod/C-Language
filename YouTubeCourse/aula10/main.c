#include <stdio.h>
int main(){
    int number1, number2, number3;
    printf("Enter with three numbers: ");
    scanf("%d%d%d", &number1, &number2, &number3);
    printf("The digited numbers were: %d, %d and %d.\n", number1, number2, number3);

    return 0;
}
