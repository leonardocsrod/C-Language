#include <stdio.h>
#include <stdlib.h>
int main(){
    int x = 3, y = 5, z; //variáveis declaradas e inicializada juntas
    z = y * x; // aqui o * é operador de multiplicação
    int *p; // aqui o * é para declarar o ponteiro
    return 0;
}
